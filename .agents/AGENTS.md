# Workspace Rules for BusinessOS AI

## Git & GitHub Workflow
- **Branch Strategy**: When pushing changes to the GitHub repository, ALWAYS create a new feature branch (e.g. `feature/day14-bi-analytics` or `day-XX-<feature>`) first and push that branch to `origin`, rather than pushing directly to `main`.
