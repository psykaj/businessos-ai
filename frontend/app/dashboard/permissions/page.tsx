import RolesPermissionsPage from "../roles/page";

export default function PermissionsPage() {
  // Alias for roles page to satisfy route requirement, or could be a distinct UI later
  return <RolesPermissionsPage />;
}
